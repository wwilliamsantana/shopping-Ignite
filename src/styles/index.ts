import { createStitches } from "@stitches/react";


export const {
  config,
  css,
  getCssText,
  keyframes,
  styled,
  theme,
  globalCss,
  createTheme
} = createStitches({
  theme:{
    colors:{
      rocketseat: "#8257e6"
    }
  }
})